import { error } from "@sveltejs/kit";

export const GET = ({}) => {
    return error(404);
}

export const POST = ({}) => {
    return error(404);
}

export const HEAD = ({}) => {
    return error(404);
}